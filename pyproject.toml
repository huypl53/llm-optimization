[project]
name = "optimization"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "llmcompressor>=0.9.0",
    "pyyaml>=6.0",
    "transformers>=4.57.3",
]

[dependency-groups]
dev = [
    "ruff>=0.14.11",
]
